<!DOCTYPE html>
<html lang="en" data-theme="corporate">
<head>
  <meta charset="UTF-8">
  <title>Activation de Compte — Portail Digital</title>
  {{ vite_entry_link_tags('app') }}
</head>
<body class="min-h-screen bg-base-200 flex items-center justify-center">
  <div class="card bg-base-100 shadow-xl w-full max-w-sm">
    <div class="card-body text-center">
      <div class="text-5xl mb-4">✅</div>
      <h1 class="text-xl font-bold mb-2">Votre Compte a Ete Activer</h1>
      <p class="text-base-content/60 text-sm mb-6">Votre mot de passe a été defini.</p>
      <a href="{{ path('auth_login') }}" class="btn btn-primary w-full">Connecter Vous →</a>
    </div>
  </div>
  {{ vite_entry_script_tags('app') }}
</body>
</html>