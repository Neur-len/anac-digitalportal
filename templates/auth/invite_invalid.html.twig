<!DOCTYPE html>
<html lang="en" data-theme="corporate">
<head>
  <meta charset="UTF-8">
  <title>Invalid Link — Digital Portal</title>
  {{ vite_entry_link_tags('app') }}
</head>
<body class="min-h-screen bg-base-200 flex items-center justify-center">
  <div class="card bg-base-100 shadow-xl w-full max-w-sm">
    <div class="card-body text-center">
      <div class="text-5xl mb-4">⛔</div>
      <h1 class="text-xl font-bold mb-2">Link expired or invalid</h1>
      <p class="text-base-content/60 text-sm mb-6">Ask an admin to resend your invite.</p>
      <a href="{{ path('auth_login') }}" class="btn btn-ghost w-full">← Login</a>
    </div>
  </div>
  {{ vite_entry_script_tags('app') }}
</body>
</html>