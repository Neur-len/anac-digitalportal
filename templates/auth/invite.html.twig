<!DOCTYPE html>
<html lang="en" data-theme="corporate">
<head>
  <meta charset="UTF-8">
  <title>Définir Mot de Passe — Portail Digital</title>
  {{ vite_entry_link_tags('app') }}
</head>
<body class="min-h-screen bg-base-200 flex items-center justify-center">
  <div class="card bg-base-100 shadow-xl w-full max-w-sm">
    <div class="card-body">
      <div class="text-center mb-4">
        <div class="text-4xl mb-2">🔑</div>
        <h1 class="text-xl font-bold">Créer votre Mot de Passe</h1>
        <p class="text-base-content/50 text-sm">{{ email }}</p>
      </div>

      {% if error %}
        <div class="alert alert-error text-sm mb-2">{{ error }}</div>
      {% endif %}

      <form method="POST" class="flex flex-col gap-3">
        <div class="form-control">
          <label class="label"><span class="label-text">Mot de Passe</span></label>
          <input type="password" name="password" required autofocus
                 minlength="8" class="input input-bordered w-full">
        </div>
        <div class="form-control">
          <label class="label"><span class="label-text">Confirmer votre Mot de Passe</span></label>
          <input type="password" name="confirm" required
                 minlength="8" class="input input-bordered w-full">
        </div>
        <button type="submit" class="btn btn-primary w-full mt-2">
          Activer votre Compte →
        </button>
      </form>
    </div>
  </div>
  {{ vite_entry_script_tags('app') }}
</body>
</html>